services:
  - type: web
    name: todo-app
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
    healthCheckPath: /

databases:
  - name: todo-db
    databaseName: todo
    user: todo_user
